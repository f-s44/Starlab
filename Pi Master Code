import smbus
import time

bus = smbus.SMBus(1)

PICO = 0x42

def read_pico(addr):
    data = bus.read_i2c_block_data(addr, 0, 4)
    return data[0] | (data[1]<<8) | (data[2]<<16) | (data[3]<<24)

while True:
    val1 = read_pico(PICO)

    print("Pico1:", val1, ")
    time.sleep(0.5)
