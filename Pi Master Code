import smbus
import struct
import time

bus = smbus.SMBus(1)

while True:
    data = bus.read_i2c_block_data(0x42, 0, 4)
    value = struct.unpack("<I", bytes(data))[0]
    print(value)
    time.sleep(0.5)
